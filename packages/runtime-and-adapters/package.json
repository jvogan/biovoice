{
  "name": "@biovoice/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist --clean --sourcemap",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
