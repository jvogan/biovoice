# Hemoglobin Structural Handoff Capture Plan

- `hero.png`: final polished frame after **Return to a fitted local heme-centered hero and export.**
- `checkpoint-1.png`: after **Open the tetramer and prediction, then save the whole-assembly overview.**
- `checkpoint-2.png`: after **Align the AlphaFold chain to chain A and store the local overlay.**

Use the verification flow to regenerate these captures after prompt or adapter changes.
