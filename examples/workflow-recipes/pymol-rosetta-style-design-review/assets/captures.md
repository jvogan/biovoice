# Rosetta-Style Design Review Capture Plan

- `hero.png`: final polished frame after **Move only the design candidate for the exploded comparison and export it.**
- `checkpoint-1.png`: after **Load the scaffold and design candidate with semantic roles.**
- `checkpoint-2.png`: after **Align the design to the scaffold and isolate the remodeled shell.**

Use the verification flow to regenerate these captures after prompt or adapter changes.
